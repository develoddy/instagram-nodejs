import { Userfollowers } from '../models/Userfollowers.js';

export const getUserFollowers = async ( req, res ) => {
};

export const createUserFollower = async ( req, res ) => {
};

export const updateUserFollowers = async ( req, res ) => {
};

export const deleteUserFollowers = async ( req, res ) => {
};

export const getUserFollower = async ( req, res ) => {
};